{
  "name": "agent-run-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A ledger for agent runs: what each one cost, where its time went, and how it ended.",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "build": "bun run --filter @ledger/console build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4",
    "typescript": "^5.9.3"
  }
}
