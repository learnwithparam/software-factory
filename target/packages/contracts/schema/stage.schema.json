{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://learnwithparam.com/schema/stage.schema.json",
  "title": "Stage",
  "description": "One timed stage inside a run. The whole point of the ledger is that these are recorded separately, because the slow one is rarely the one people assume.",
  "type": "object",
  "additionalProperties": false,
  "required": ["runId", "name", "startedAt", "endedAt", "toolCalls"],
  "properties": {
    "runId": { "type": "string" },
    "name": { "type": "string", "enum": ["claim", "context", "implement", "gates", "verify", "human"], "description": "Which stage of the loop this is" },
    "startedAt": { "type": "string" },
    "endedAt": { "type": "string" },
    "toolCalls": { "type": "integer", "description": "Tool calls the agent made during this stage" }
  }
}
