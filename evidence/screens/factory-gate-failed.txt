Skip to content
Navigation Menu
Platform
Solutions
Resources
Open Source
Enterprise
Pricing
Search
/
Sign in
Sign up
learnwithparam
/
agent-run-ledger
Public
Notifications
Fork 0
 Star 0
Code
Issues
7
Pull requests
6
Actions
Projects
Security and quality
Insights
Never report a negative remaining balance
- #604
#604
Open
Param-Harrison
wants to merge 1 commit into
main
from
gate/remaining-clamped
+1
-1
Lines changed: 1 addition & 1 deletion
Conversation2
 (2)
Commits1
 (1)
Checks0
 (0)
Files changed1
 (1)
Conversation
Param-Harrison
commented
Contributor

A run that overspends currently reports a negative remaining figure, which reads badly in the console. Clamps it at zero.

Never report a negative remaining balance
7046a8e
Param-Harrison
commented
Contributor
Author

Verdict: request changes

Findings
Correctness — blocking

Two existing tests fail with this change, and their failure is not a fragile assertion — they encode the documented invariant of the module:

services/budget/tests/budget.rs:36-40 — overspend_is_reported_rather_than_clamped asserts remaining_minor == -23_000 when spending 73_000 against a 50_000 limit. The PR clamps remaining to 0, producing a test failure (left: 0, right: -23000).

services/budget/tests/budget.rs:4-13 — spent_plus_remaining_always_equals_the_limit asserts spent_minor + remaining_minor == limit_minor for every spend case. With remaining clamped at 0 and spent at 73_000, the invariant becomes 73_000 != 50_000 — the module-level invariant spent + remaining == limit (lib.rs:5-6) is broken.

Documentation inconsistency

The assess() docstring (lib.rs:98-99) explicitly states: "Overspend is reported as a negative remainder rather than clamped to zero, because hiding it is how a breach goes unnoticed." The PR changes the behavior without updating this docstring.

Test gap

No new tests cover the clamped-at-zero behavior. No existing tests are updated to match the changed semantics. No verification evidence is provided in the PR.

Issue context gap

The PR has no closing issue reference and no related issue that provides context or approval for this design change. The PR body describes it as a cosmetic improvement ("reads badly in the console"), but the existing design deliberately surfaces negative remaining as an explicit signal — the format_minor function already handles negative values.

Charter considerations

The charter marks services/budget/** as a protected path (the money path, never modified by an agent unattended). Weakening existing test assertions is also a protected operation per the charter. This change touches both.

Issue and intent
Classification: Maintenance/behavior change (not a bug fix — the existing behavior is intentional and documented)
Related issue: None. No closing issue or context issue exists.
Labels: None on the PR.
Scope match: The implemented behavior (clamping remaining to 0) does not match what would be expected from the existing documented contract. The PR title "Never report a negative remaining balance" is accurate for what the code does, but the existing test and doc contract explicitly expects negative reporting.
Verification

Executed from services/budget/ on the PR head commit (7046a8e):

cargo build -p budget → builds successfully
cargo test → 2 of 11 tests FAILED:
overspend_is_reported_rather_than_clamped — panics at assertion left: 0, right: -23000
spent_plus_remaining_always_equals_the_limit — panics at invariant broken at spend 73000, left: 73000, right: 50000
9 tests pass (unaffected: validation, state logic, formatting, currency parsing)

Commands run: env -u GH_TOKEN -u GITHUB_TOKEN cargo test (inside services/budget/)

Existing review disposition

No existing reviews, comments, or review threads found on the PR.

Requested changes

Update tests to match the new behavior. If the change is intentional, the two failing tests must be updated and a new test should verify the clamped-at-zero boundary (spent == limit should still yield remaining == 0 as it does today; spent > limit should yield remaining == 0). The spent_plus_remaining_always_equals_the_limit invariant test must be revised or removed — its assertion no longer holds for overspend cases, and removing it requires justification.

Update the assess() docstring (lib.rs:98-99) to describe the clamped behavior instead of the negative-reporting behavior. Clarify why clamping is preferred over the previous explicitly-chosen design.

Update the module-level invariant documentation (lib.rs:5-6) to reflect that spent + remaining == limit holds only when spend does not exceed the limit.

Alternatively, if the existing design of negative remaining is intentional and correct (which the tests and docs assert), close this PR without merging.

Assumptions
The author's intent is cosmetic — "reads badly in the console" — not a correctness fix. The existing format_minor function already handles negative values, so this is a design preference, not a bug.
The PR is a first-time contribution from this author (0 merged PRs), which warrants closer scrutiny of the design rationale.
The absence of an issue means this design change was not discussed or approved before implementation.
Open questions
Should the console display logic be updated instead of the money path? For example, the console could format overspend as "€0.00 remaining (overspent by €230.00)" without changing the budget engine's invariant.

Review runtime: openrouter/deepseek/deepseek-v4-flash, reasoning setting: off.

 Param-Harrison added the status:changes-requested label 
Param-Harrison
commented
Contributor
Author

Nothing has changed since my last review pass. The previous verdict (request changes) still stands: 2 tests fail, the documented invariant is broken, and the design change lacks issue context. The three requested changes are still unaddressed.

Sign up for free to join this conversation on GitHub. Already have an account? Sign in to comment
Reviewers
No reviews
Assignees
No one assigned
Labels
status:changes-requested
Projects
None yet
Milestone
No milestone
Development

Successfully merging this pull request may close these issues.

None yet

1 participant
Footer
© 2026 GitHub, Inc.
Footer navigation
Terms
Privacy
Security
Status
Community
Docs
Contact
Manage cookies
Do not share my personal information