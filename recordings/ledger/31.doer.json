{
  "$comment": "The cross-stack change: one shared definition, three languages.",
  "text": "Added toolCalls to the run schema, then to the TypeScript type and its field list, the Go struct and its json tag, and the console total. The Rust side reads runs only through the contract test, which picked the new property up without a change. Three suites pass together.",
  "toolCalls": 31,
  "tokensIn": 88400,
  "tokensOut": 12900,
  "costMinor": 168,
  "changedFiles": [
    "packages/contracts/schema/run.schema.json",
    "packages/contracts/src/index.ts",
    "services/ingest/run.go",
    "apps/console/app/page.tsx"
  ]
}
