{
	"$comment": "The specs are the most-edited code in this repository and nothing typechecked them: the root tsconfig excludes e2e, so a duplicate declaration survived make check and died seventeen minutes into a run. make check typechecks this project too now.",
	"compilerOptions": {
		"target": "ES2022",
		"module": "Preserve",
		"moduleResolution": "bundler",
		"allowImportingTsExtensions": true,
		"strict": true,
		"noUncheckedIndexedAccess": true,
		"noEmit": true,
		"skipLibCheck": true,
		"types": [
			"bun",
			"node"
		],
		"typeRoots": [
			"../node_modules/@types",
			"node_modules/@types"
		]
	},
	"include": [
		"lib",
		"specs"
	]
}
